'use client';

import React from 'react';
import { Search } from 'lucide-react';

export default function SearchIcon() {
  return (
    <Search size={16} className="text-neutral-500 shrink-0 group-focus-within:text-[#D4AF37] transition-colors" />
  );
}