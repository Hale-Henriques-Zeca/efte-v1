import Panel from "@/components/ui/Panel";

export default function Chart() {
  return (
    <Panel title="Gráfico">
      <div className="flex h-[400px] items-center justify-center text-sm text-[var(--text-muted)]">
        TradingView (em breve)
      </div>
    </Panel>
  );
}
